FORCE_GYM_API_BASE=http://localhost:8080/api
NEXT_PUBLIC_FORCE_GYM_API_BASE=/api

# ── Base de datos Railway (para el backend) ──────────────────
# Define estas variables en tu entorno antes de correr start-all.ps1
# para conectar al PostgreSQL de Railway en vez del local
#
# $env:DB_URL      = "jdbc:postgresql://roundhouse.proxy.rlwy.net:34284/railway"
# $env:DB_USERNAME = "postgres"
# $env:DB_PASSWORD = "TU_PASSWORD_RAILWAY"
